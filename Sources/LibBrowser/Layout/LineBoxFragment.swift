extension Layout {
    class LineBoxFragement {
        let layoutNode: Layout.Node? = nil
        let start = 0
        let length = 0
        let borderBoxTop = CSS.Pixels(0)
        let borderBoxBottom = CSS.Pixels(0)
        let baseline = CSS.Pixels(0)
    }
}
