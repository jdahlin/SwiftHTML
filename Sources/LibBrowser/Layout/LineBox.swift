extension Layout {
    class LineBox {
        let width = CSS.Pixels(0)
        let height = CSS.Pixels(0)
        let bottom = CSS.Pixels(0)
        let baseline = CSS.Pixels(0)
        let fragments: [LineBoxFragement] = []
    }
}
